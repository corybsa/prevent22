const config = {
    appSecret: 'We are what we repeatedly do. Excellence, then, is not an act, but a habit.',
    sql: {
        user: 'webuserdev',
        password: 'E48NSG7%Nt9mDTRtF3SWJKirN^&ZV8',
        server: 'prevent22.cvedbc6ysvqp.us-east-2.rds.amazonaws.com',
        database: 'Prevent22Dev'
    }
};

// TODO: check for dev and prod

module.exports = config;
